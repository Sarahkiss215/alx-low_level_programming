#include <stdio.h>

/**
 * main - prints all single digits of base 10
 * followed by a new line
 *
 * Return: Always 0
 */
int main(void)
{
	int num;

	num = 0;
	while (num < 10)
	{
		printf("%d\n", num);
	}
	return (0);
}
